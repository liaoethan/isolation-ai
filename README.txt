Extract the ZIP file and locate the src folder inside with the command prompt.

Executing the program:
1) Use 'cd' in command prompt into the 'src' folder
2) Enter: javac Main.java
3) Enter: java Main
